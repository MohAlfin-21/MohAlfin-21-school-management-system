#pragma once

// Copy this file to `secrets.h` and fill your real values.
// NOTE: `secrets.h` is gitignored to prevent leaking credentials.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
#define DEVICE_TOKEN "YOUR_DEVICE_TOKEN"

